{
  "name": "seeds-tuition-backend",
  "version": "2.0.0",
  "description": "Seeds Tuition — Vercel serverless backend for payments, calendar, reminders",
  "private": true,
  "scripts": {
    "test": "node --test"
  },
  "dependencies": {
    "nodemailer": "^6.9.7",
    "raw-body": "^2.5.2",
    "stripe": "^14.0.0"
  }
}
